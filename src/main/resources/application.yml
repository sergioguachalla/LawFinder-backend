server:
  port: 8080
spring:
  application:
    name: law-finder
  datasource:
    url: ${POSTGRES_DB_URL:jdbc:postgresql://localhost:5433/law-finder}
    username: ${POSTGRES_USERNAME:postgres}
    password: ${POSTGRES_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  # sql:
  #   init:
  #     mode: always
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
#  mail:
#    host: smtp.gmail.com
#    port: 587
#    username: law.finder.ucb@gmail.com
#    password: zxtgwqffbbgubfbg
  mail:
    host: localhost
    port: 1025
    username: law.finder.ucb@gmail.com
    password: password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
